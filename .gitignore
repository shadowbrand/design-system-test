node_modules
.DS_Store

# esbuild preview build output (dev-only, regenerated by `npm run preview`)
packages/ui/preview/app.js
packages/ui/preview/app.css

# consumer-check installs ui via file: — its lock churns on every ui version
# bump, so don't track it (npm install re-resolves fresh).
consumer-check/package-lock.json
